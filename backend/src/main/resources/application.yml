server:
  port: 8090

spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/splashcan
    username: splashcan
    password: splashcan
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: ${JWT_SECRET:NFy4tqcu7GunlxYq3lPjBis47KkVXvGQqzB5SnNy6DVJjAXxOExINKtQHXHflGzM}
    access-token-expiration-minutes: 15
    refresh-token-expiration-days: 7
